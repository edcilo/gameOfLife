import { StyleSheet, View } from "react-native";

const style = StyleSheet.create({
    col: {
        //padding: 16,
        alignItems: "center",
    },
    container: {
        flexDirection: "row",
        flexWrap: "wrap",
        paddingTop: 8,
        paddingBottom: 8,
        width: 304
    }
});

export default style;
